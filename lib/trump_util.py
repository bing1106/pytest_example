import numpy as np


def add_two_num(a: int, b: int)->int:
    return a + b


def concat_two_strings(s1: str, s2: str)->str:
    return s1 + s2
    
    
def calc_abs(num):
    return np.absolute(num)


def msg_len(msg):
    return len(msg)